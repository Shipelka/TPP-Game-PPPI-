class HUD:
    """
    Класс для отображения интерфейса пользователя (HUD) в игре.

    Атрибуты:
        player (Player): Объект игрока, для которого отображаются данные в интерфейсе.

    Методы:
        display(): Отображает текущее состояние игрока (здоровье, щит, валюта, статистика).
        update(): Обновляет интерфейс (логика обновления данных HUD).
    """

    def __init__(self, player):
        """
        Инициализация HUD для конкретного игрока.

        Аргументы:
            player (Player): Объект игрока, чьи данные будут отображаться на HUD.
        """
        self.player = player

    def display(self):
        """
        Отображает текущие данные игрока в интерфейсе.

        Информация, которая отображается:
            - HP (здоровье)
            - Shield (щиты)
            - Currency (валюта)
            - Kills (убийства)
            - Deaths (смерти)
            - Assists (помощь)

        Пример вывода:
            HP: 100 | Shield: 50 | Currency: 200
            Kills: 5 | Deaths: 3 | Assists: 2
        """
        print(f"HP: {self.player.hp} | Shield: {self.player.shield} | Currency: {self.player.currency}")
        print(f"Kills: {self.player.kills} | Deaths: {self.player.deaths} | Assists: {self.player.assists}")

    def update(self):
        """
        Логика обновления HUD. Этот метод можно расширить для динамического обновления данных
        (например, по мере изменения состояния игрока или события в игре).

        В текущем виде метод не выполняет никаких операций, но может быть использован в будущем
        для обработки обновлений интерфейса.
        """
        pass
